# 翻译桥的运行依赖（Windows）。
#
# 只有这几样：桥 = 一个本机 HTTP 服务 + 一个 OpenAI 兼容客户端。
# 桌面版那一stack（PySide6 / numpy / Pillow / mss / pyperclip / keyboard /
# pywin32 / rapidocr / dxcam）已随桌面版代码一起移除，理由见 pyproject.toml。
openai>=1.0
pydantic>=2.0
PyYAML>=6.0
